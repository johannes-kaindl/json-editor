# 1.3.0 — JSON Schema Validation — Implementation Plan

Spec: `docs/superpowers/specs/2026-05-27-1.3.0-schema-validation-design.md`

TDD throughout.

---

## Phase 1 — Core schema module

- [ ] **1.1** `tests/core/schema.test.ts` — compileSchema + validate
- [ ] **1.2** `src/core/schema.ts` — compileSchema, validate, PathError, instancePath → JsonPath helper

## Phase 2 — Settings

- [ ] **2.1** Extend `JsonEditorSettings` with `validateAgainstSchema: boolean` + `companionSchemaSuffix: string`. Update DEFAULT_SETTINGS.
- [ ] **2.2** Add toggle + text input to `SettingsTab.ts`. Test update.

## Phase 3 — SchemaBanner

- [ ] **3.1** `tests/obsidian/SchemaBanner.test.ts`
- [ ] **3.2** `src/obsidian/SchemaBanner.ts`

## Phase 4 — TreeView row-error marking

- [ ] **4.1** Extend `TreeView.test.ts`: `setValidationErrors(map)` adds .json-row-error class + title; clearing removes
- [ ] **4.2** Implement in `TreeView.ts`

## Phase 5 — JsonFileView integration

- [ ] **5.1** `tests/obsidian/JsonFileView.schema.test.ts` — setSchema + validation pushed to UI + settings-toggle disables
- [ ] **5.2** Implement schema state + applyValidation in `JsonFileView.ts`. Wire companion-load best-effort (don't crash if `this.file` is null or vault has no sibling).

## Phase 6 — Styles

- [ ] **6.1** `.json-schema-banner`, `.json-row.json-row-error` blocks

## Phase 7 — Release

- [ ] **7.1** All tests + build green
- [ ] **7.2** CHANGELOG + version bump
- [ ] **7.3** Commits + merge + tag + push both remotes
- [ ] **7.4** Test-vault deploy
- [ ] **7.5** AGENTS.md update
