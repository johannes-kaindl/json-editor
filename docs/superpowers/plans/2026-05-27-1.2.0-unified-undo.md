# 1.2.0 — Cross-mode unified Undo/Redo — Implementation Plan

Spec: `docs/superpowers/specs/2026-05-27-1.2.0-unified-undo-design.md`

TDD: tests first.

---

## Phase 1 — Generic History

- [ ] **1.1** Refactor `History` → `History<T>`
  - Update `tests/core/history.test.ts` to use `History<string>` (and confirm no required interface change beyond type)
  - Refactor `src/core/history.ts`: parametrize on `T`; drop `HistoryState` interface (or keep as deprecated re-export — actually delete since no external consumers)

## Phase 2 — JsonFileView text-history

- [ ] **2.1** Migrate `JsonFileView.history` from `History<HistoryState>` to `History<string>`
  - applyMutation pushes pre-text
  - undo/redo restore text
  - canUndo/canRedo no longer mode-gated
- [ ] **2.2** Source-mode pushes pre-text on each `handleSourceChange`
- [ ] **2.3** Remove `this.history.clear()` from `switchTo`
- [ ] **2.4** Update `tests/obsidian/JsonFileView.undo.test.ts`:
  - any test asserting "mode switch clears history" inverts to "preserves"
  - add: source-mode undo, source-mode redo, cross-mode undo (tree → source → undo), cross-mode undo (source → tree → undo), canUndo true in source after change

## Phase 3 — SourceView no longer keeps local CM history

- [ ] **3.1** Remove `history()` + `historyKeymap` from `SourceView` extensions
- [ ] **3.2** Existing source tests should still pass (no behavior change beyond losing CM-local undo)

## Phase 4 — main.ts command rename

- [ ] **4.1** Rename `undo-tree-edit` → `undo-edit`, `redo-tree-edit` → `redo-edit`
- [ ] **4.2** No other consumers — done

## Phase 5 — Release

- [ ] **5.1** All tests + build green
- [ ] **5.2** CHANGELOG entry + version bump (1.2.0)
- [ ] **5.3** Commits, merge --no-ff, tag, push both remotes
- [ ] **5.4** Test-vault deploy
- [ ] **5.5** AGENTS.md update
