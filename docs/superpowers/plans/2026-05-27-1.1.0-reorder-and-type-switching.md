# 1.1.0 — Drag-Drop Reorder + Type-Switching — Implementation Plan

Spec: `docs/superpowers/specs/2026-05-27-1.1.0-reorder-and-type-switching-design.md`

TDD: every step starts with a failing test.

---

## Phase 1 — Core ops

- [ ] **1.1** Add `moveArrayItem` + tests
  - Write `tests/core/edit.move.test.ts` (≥6 cases: simple move down/up, edge indices, no-op same-idx, out-of-bounds, immutability)
  - Implement `moveArrayItem` in `src/core/edit.ts`
- [ ] **1.2** Add `moveObjectKey` + tests
  - Extend `tests/core/edit.move.test.ts` (≥6 cases: move first→last, last→first, middle move, key not found, no-op, immutability, insertion order preserved for non-moved keys)
  - Implement `moveObjectKey`
- [ ] **1.3** Add `changeType` + tests
  - Write `tests/core/edit.changeType.test.ts` (≥12 cases: all 6 source × target conversions; defaults; root-path; no-op same-type; immutability)
  - Implement `changeType` and `JsonType` export

## Phase 2 — Pure helpers

- [ ] **2.1** `computeInsertionIndex(fromIdx, toIdx, dropPosition)`
  - Write tests in `tests/core/edit.move.test.ts`
  - Cases: drop-before-self, drop-after-self, before-target (other), after-target (other), array vs index semantics
  - Implement in `src/core/edit.ts` as utility — covers same-parent index math after splice-removal

## Phase 3 — UI components

- [ ] **3.1** `TypeMenu` component
  - Write `tests/obsidian/TypeMenu.test.ts`: renders 6 buttons, active type disabled, click → callback + close, ESC closes, click-outside closes
  - Implement `src/obsidian/TypeMenu.ts`
- [ ] **3.2** `RowActions` adds type-button
  - Extend `tests/obsidian/RowActions.test.ts`: assert "T" button + onChangeType invocation
  - Extend `RowActions.ts` with `onChangeType?: () => void` opt
- [ ] **3.3** Drag-handle helper in TreeView
  - Write `tests/obsidian/TreeView.dragdrop.test.ts`: prepend `.json-drag-handle` to rows; row is `draggable=true`; dragstart sets payload; same-parent drop fires `onMoveItem`/`onMoveKey`; cross-parent drop is no-op; computeInsertionIndex used correctly
  - Implement drag wiring in `src/obsidian/TreeView.ts`

## Phase 4 — JsonFileView integration

- [ ] **4.1** Wire `onMoveItem`/`onMoveKey`/`onChangeType` handlers
  - Extend `tests/obsidian/JsonFileView.test.ts` (or new `JsonFileView.reorder.test.ts`): verify move + changeType apply via `applyMutation` and push history
  - Implement `handleMoveItem`/`handleMoveKey`/`handleChangeType` in `src/obsidian/JsonFileView.ts`

## Phase 5 — Styles

- [ ] **5.1** Drag-handle, drop-target visual indicator, dragging state, type-menu popover
  - `styles.css` additions; smoke-test via build only (visual sign-off deferred to manual smoke-test)

## Phase 6 — Release

- [ ] **6.1** `npm test` all green, `npm run build` clean, coverage rerun
- [ ] **6.2** CHANGELOG entry + bump `package.json` + `manifest.json` + `versions.json` to `1.1.0`
- [ ] **6.3** Commit, merge `--no-ff` into `main`, tag `1.1.0`, push to `origin` + `github`
- [ ] **6.4** Copy `main.js manifest.json styles.css` to test vault `X1_v6t2b9`
- [ ] **6.5** Update `AGENTS.md` session history + state
